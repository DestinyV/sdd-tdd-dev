# 基础设施分析

## 1. 服务上下游依赖

### 1.1 上游调用方
| 调用方 | 调用方式 | 接口端点 | 备注 |
|--------|---------|---------|------|
| [上游服务A] | HTTP/gRPC/MQ | POST /api/xxx | [说明调用场景] |
| [前端应用] | HTTP | GET /api/xxx | [说明调用场景] |

### 1.2 下游依赖服务
| 依赖服务 | 依赖方式 | 用途 | 容错策略 |
|---------|---------|------|---------|
| [下游服务A] | HTTP/RPC | [用途说明] | 超时/重试/降级 |
| [Redis] | 客户端 | [用途说明] | 降级方案 |

### 1.3 接口契约变更
| 接口 | 变更类型 | 变更内容 | 影响范围 |
|------|---------|---------|---------|
| POST /api/xxx | 新增/修改 | 新增字段xxx | 调用方xxx |

---

## 2. 数据库表设计改动

> 💡 **提示**：完整的可执行 SQL DDL/DML 脚本见 `sql-ddl.md`（由 code-designer 阶段生成）。
> 本章节定义逻辑表结构设计，完整 SQL 实现见 sql-ddl.md。

### 2.1 新增表
**表名**：`[table_name]`
| 字段名 | 类型 | 必需 | 约束 | 说明 |
|--------|------|------|------|------|
| id | bigint | Y | PRIMARY KEY AUTO_INCREMENT | 唯一标识 |
| created_at | datetime | Y | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

**索引**：
- PRIMARY KEY: id
- INDEX: xxx (查询优化)

### 2.2 现有表变更
**表名**：`[table_name]`
- **新增字段**：[字段名] [类型] [约束] [说明]
- **修改字段**：[字段名] [旧类型→新类型] [原因]
- **删除字段**：[字段名] [原因]（⚠️ 确认无引用）
- **新增索引**：INDEX idx_xxx (column1, column2)
- **删除索引**：INDEX idx_xxx

### 2.3 数据迁移
- **迁移脚本**：是否需要？[是/否]
- **历史数据处理**：[处理方案]
- **停机时间**：是否需要？预计多久？

### 2.4 事务和一致性
- **事务范围**：[哪些操作需要事务]
- **一致性要求**：强一致性 / 最终一致性
- **并发控制**：乐观锁 / 悲观锁 / 无

---

## 3. 中间件和基础设施

### 3.1 Redis
| 用途 | Key格式 | Value类型 | TTL | 淘汰策略 |
|------|---------|----------|-----|---------|
| [缓存xxx] | `app:module:{id}` | Hash | 3600s | LRU |
| [分布式锁] | `lock:module:{id}` | String | 30s | - |

### 3.2 Nacos
| 配置项 | Key | 默认值 | 说明 |
|--------|-----|--------|------|
| [配置名] | `module.xxx.enabled` | `true` | [用途说明] |

### 3.3 Docker/K8s
- **Dockerfile变更**：[新增依赖/环境变量/端口]
- **Deployment变更**：[副本数/资源限制/环境变量]
- **ConfigMap/Secret变更**：[新增配置项]
- **Service变更**：[新增/修改Service端口]

---

## 4. 项目配置

### 4.1 环境变量
| 变量名 | 环境 | 必需 | 默认值 | 说明 |
|--------|------|------|--------|------|
| XXX_API_URL | dev/test/prod | Y | - | [说明] |

### 4.2 新增依赖
| 依赖 | 版本 | 用途 | 替代方案评估 |
|------|------|------|-------------|
| [包名] | ^x.y.z | [用途] | [是否评估过替代品] |

### 4.3 构建配置变更
- [是否需要修改webpack/vite/tsconfig等]
- [变更内容和原因]

---

## 5. 标准日志规范

### 5.1 日志框架
- 日志框架：[如 winston / log4j / zap]
- 日志格式：[JSON / 文本]
- 日志级别策略：DEBUG/INFO/WARN/ERROR

### 5.2 关键业务日志
| 日志点 | 级别 | 日志内容 | 上下文字段 |
|--------|------|---------|-----------|
| [操作开始] | INFO | [描述] | userId, requestId |
| [操作成功] | INFO | [描述] | userId, requestId, result |
| [操作失败] | ERROR | [描述] | userId, requestId, error, stack |

### 5.3 链路追踪
- 是否需要 traceId：[是/否]
- traceId 生成和传递方式：[HTTP header / MQ header]
- 关键链路节点：[入口 → 服务A → 服务B → DB]

### 5.4 错误日志标准
错误日志必须包含：
- 时间戳
- 请求ID（traceId）
- 用户ID（如适用）
- 错误堆栈
- 关键业务上下文
