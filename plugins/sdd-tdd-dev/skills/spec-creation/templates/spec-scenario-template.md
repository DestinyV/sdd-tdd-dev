# 场景: [场景名称]

**优先级**：[高/中/低]
**相关场景**：[相关的其他场景]
**场景间依赖** ⭐🆕：
- depends-on: [依赖的场景ID或名称，如无留空]
- provides-to: [为哪些场景提供数据/状态，如无留空]

---

## 场景描述

[场景的详细描述，说明这个场景的业务意义和用户目标]

---

## 前置条件

- [系统状态条件1]
- [用户权限条件]
- [数据条件]

---

## Case 1: [case名称]

**目标**：[这个case要验证什么]

**WHEN** (触发条件)
```
- 条件1：[具体条件]
- 条件2：[具体条件]
- 条件3：[具体条件]
```

**THEN** (预期结果)
```
- 结果1：[具体的预期结果]
  - 例：显示[提示信息]
  - 例：跳转到[页面/流程]
  - 例：数据库记录[操作]
- 结果2：[具体的预期结果]
```

**数据**：
```json
// 输入数据示例
{
  "field1": "value1",
  "field2": "value2"
}

// 期望的返回数据
{
  "status": "success",
  "data": {
    // ...
  }
}
```

**BROWSER-TESTABLE**（前端/全栈场景必填）：
```
// 定义浏览器级别的可验证验收标准
// 每个 THEN 结果都应对应具体的浏览器可执行断言

URL 验证:
  - 页面 URL 变为 '[expected-path]'
  - 页面 URL 包含 '[query-param]'

DOM 元素验证:
  - 页面包含 [data-testid="expected-element-id"] 元素
  - [data-testid="expected-element-id"] 元素可见/不可见
  - 页面包含 [data-testid="expected-element-id"] 元素，文本为 "[expected-text]"

交互验证:
  - 点击 [data-testid="click-target"] 后，页面显示 [expected-result]
  - 提交表单后，[data-testid="form"] 显示错误提示 "[error-message]"

状态验证:
  - Cookie 中存在 [cookie-name]
  - localStorage 中 [key] 的值为 "[value]"
```

**备注**：[任何额外说明]

---

## Case 2: [case名称]

[按同样格式重复]

---

## Case N: 异常情况

### Case N.1: [异常1]

**WHEN**
```
- 用户[操作]时
- 系统[异常情况]
```

**THEN**
```
- 显示错误提示：[提示内容]
- 恢复到[状态]
- 记录日志：[日志内容]
```

### Case N.2: [异常2]

[按同样格式重复]

---

## 验证清单

- [ ] 所有Case都明确了WHEN条件
- [ ] 所有Case都定义了THEN结果
- [ ] 异常情况都已考虑
- [ ] 数据示例真实可行
- [ ] 与其他场景无冲突

---

## 设计决策（快速模式必填）

> 此部分仅在快速模式下填写，用于替代独立的 design.md 文档。
> 标准模式可留空或删除此部分。

**组件/模块结构**：
- [选择的结构方案及原因]

**关键实现决策**：
1. [决策1]: [原因]
2. [决策2]: [原因]

**样式/交互方案**：
- [样式选择]
- [关键交互处理]
