# 设计文档模板

```markdown
# [功能名称] 设计方案

**目标**：[功能目标和意义]
**设计基准**：[对标的参考组件]
**技术栈**：[涉及的主要技术栈]
**相关规范**：[spec/requirement.md](../spec/requirement.md)

---

## 快速导航

- [需求分析](#1-需求分析) - 核心功能和用户场景
- [项目文档约束](#20-项目文档约束-⭐🆕) - 项目自述架构约定
- [设计方案](#2-设计方案) - 架构模式、设计模式、分层设计、组件、数据流
  - [2.1 整体架构](#21-整体架构)
  - [2.1.1 架构模式选择 ⭐🆕](#211-架构模式选择) - 架构模式、依赖规则图
  - [2.2 设计模式应用 ⭐🆕](#22-设计模式应用) - 设计模式清单、使用位置
  - [2.3 分层设计与依赖规则 ⭐🆕](#23-分层设计与依赖规则) - 层定义、依赖方向、禁止依赖
  - [2.4 核心模块设计](#24-核心模块设计) - 前端组件、后端服务、数据库
  - [2.5 状态管理](#25-状态管理)
  - [2.6 数据流](#26-数据流)
- [与参考设计的对比](#3-与参考设计的对比) - 创新点和差异说明
- [技术方案](#4-技术方案) - 依赖、API、性能考虑
- [实现清单](#5-实现清单) - 核心组件清单
- [设计决策记录](#6-设计决策记录) - 关键决策和原因
- [潜在风险和注意事项](#7-潜在风险和注意事项) - 可能的问题和缓解方案
- [组件/模块抽离策略 ⭐🆕](#215-组件模块抽离策略) - 组件拆分决策、Props/Events 设计
- [领域模型设计 ⭐🆕（后端）](#216-领域模型设计后端) - 聚合根、实体、值对象、领域服务
- [数据库设计](#28-数据库设计) ⭐ - 表结构、索引、迁移、事务、SQL 执行计划
- [接口契约](#210-接口契约) ⭐🆕 - API 端点、请求/响应、错误码、Mock 数据
- [基础设施设计](#27-服务上下游设计) ⭐ - 服务依赖、中间件、日志
- [监控和上线方案](#44-监控设计) ⭐ - 监控指标、上线顺序、回滚
- [前端构建和部署](#211-前端构建设计) ⭐ - 构建优化、CDN、缓存、回滚
- [前端监控方案](#46-前端监控设计) ⭐ - 错误追踪、性能指标、埋点
- [测试性设计](#213-测试性设计) ⭐ - data-testid 策略、可测试交互、视觉回归关键点

---

## 1. 需求分析

### 核心功能
- [功能点1]
- [功能点2]

### 用户场景
- 场景1：[用户在什么情况下会使用？]
- 场景2：...

---

## 2. 设计方案

### 2.1 整体架构
[ASCII图或简化的架构说明]

### 2.1.1 架构模式选择 ⭐🆕

**选择的架构模式**：[Clean Architecture / Hexagonal / Layered / MVC / MVVM / CQRS / ...]
**选择原因**：[为什么选择此架构模式]

**依赖规则图**：
```
[ASCII 图展示各层之间的依赖方向]
```

**依赖规则**：
- 允许：[哪些层可以依赖哪些层]
- 禁止：[哪些层不能依赖哪些层]

### 2.2 设计模式应用 ⭐🆕

**将使用的设计模式清单**：

| 模式 | 使用位置 | 使用原因 |
|------|---------|---------|
| [Strategy] | [PaymentService] | [需要运行时切换支付方式] |
| [Observer] | [OrderEventEmitter] | [订单状态变化需要通知多个模块] |

### 2.3 分层设计与依赖规则 ⭐🆕

**分层定义**：

| 层 | 职责 | 文件路径 |
|---|------|---------|
| **表现层** | [接收请求、返回响应] | [src/controllers/] |
| **应用层** | [用例编排、事务管理] | [src/services/] |
| **领域层** | [业务规则、领域模型] | [src/domain/] |
| **基础设施层** | [数据访问、外部服务] | [src/repositories/] |

**层间依赖方向**：
- 表现层 → 应用层
- 应用层 → 领域层 + 基础设施层（通过接口）
- 领域层 → 不依赖任何层
- 基础设施层 → 领域层（通过接口实现）

**禁止的依赖**：
- ❌ [数据层不能调用业务层]
- ❌ [表现层不能直接调用数据层]

### 2.4 核心模块设计

**前端组件设计**：
#### 主组件：[组件名]
- **Props**：
  ```typescript
  interface [ComponentName]Props {
    // 参考设计中的Props
    // 新增的Props
  }
  ```
- **关键特性**：
  - 特性1（参考自 [参考组件]）
  - 特性2（新增功能）

**后端服务设计**：
#### 核心服务：[服务名]
- **职责**：[服务的主要职责]
- **API端点**：
  - `GET /api/resource` - 获取资源列表
  - `POST /api/resource` - 创建资源
  - `PUT /api/resource/:id` - 更新资源
  - `DELETE /api/resource/:id` - 删除资源

**数据库设计**：
#### 数据模型：[模型名]
- **表/集合**：[数据库表或集合名]
- **字段设计**：[主要字段和类型]
- **索引策略**：[性能优化索引]
- **约束条件**：[业务约束和一致性规则]

### 2.5 状态管理
- **本地状态**：使用 [useState / useReducer]
- **共享状态**：[Redux / Zustand / Context API]
- **状态流**：[状态转移图或说明]

### 2.6 数据流
```
用户交互 → 事件处理 → 状态更新 → 视图重新渲染
```

### 2.0 项目文档约束 ⭐🆕（fullstack 模式必填）

> 来自 requirement.md 中「项目文档约束」章节，设计时必须引用且不违背。

**项目自述架构**：
- 架构模式：[从 requirement.md 中提取]
- 模块划分：[从 requirement.md 中提取]
- 技术栈：[从 requirement.md 中提取]

**关键约定**：
- [约定1，如"领域层零外部依赖"]
- [约定2]
- [约定3]

**设计与项目约定的对齐情况**：
- [ ] 新架构符合项目声明的架构模式
- [ ] 新增模块边界符合项目声明的模块划分
- [ ] 新技术选型与项目声明的技术栈兼容
- [ ] 编码方式遵循项目声明的关键约定

### 2.5 现有项目约定 ⭐🆕（fullstack 模式必填）

> 由 code-architect agent 扫描现有代码库后填入，fullstack 模式必填。

#### URL 风格
- 基础路径前缀：[例如 /api/v1]
- 命名规范：[例如 复数小写]

#### 响应格式
- 成功：[例如 { code: 0, data: {...}, message: "success" }]
- 失败：[例如 { code: xxx, message: "错误描述", data: null }]

#### 认证方式
- [例如 Header: Authorization: Bearer {token}]

#### 已有中间件/工具
- [例如 全局异常处理中间件]
- [例如 请求日志中间件]
- [例如 Redis 缓存策略]

### 2.6 接口清单初稿 ⭐🆕（fullstack 模式必填）

> fullstack 模式必填。作为 api-contract 阶段的输入基础。

| 接口路径 | HTTP 方法 | 功能描述 | 关联数据库表 |
|---------|----------|---------|-------------|
| | | | |

简要说明：
- 请求/响应描述：
- 与数据库表的映射关系：

### 2.7 服务上下游设计 ⭐（后端/全栈场景）

**上游调用方**：
| 调用方 | 接口 | 协议 | 鉴权 |
|--------|------|------|------|
| [上游服务A] | POST /api/xxx | HTTP/gRPC | [鉴权方式] |

**下游依赖服务**：
| 依赖服务 | 调用方式 | 超时配置 | 重试策略 | 降级方案 |
|---------|---------|---------|---------|---------|
| [下游服务A] | HTTP/gRPC | 5s | 3次,指数退避 | [降级逻辑] |

**接口契约变更**：
| 接口 | 变更类型 | 变更内容 | 影响范围 | 兼容性处理 |
|------|---------|---------|---------|-----------|
| POST /api/xxx | 新增/修改 | 新增xxx字段 | 调用方xxx | 默认值兼容 |

### 2.8 数据库设计 ⭐（后端/全栈场景，needs-database: true 时必填）

> 当 needs-database 为 false 时可跳过此章节。

> ⚠️ **前后端都改动时，此章节必须在接口契约之前完成并确认。**

**新增表**：
```sql
CREATE TABLE [table_name] (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

**表结构变更**：
```sql
ALTER TABLE [table_name] ADD COLUMN [column_name] [type] [constraint] COMMENT '[说明]';
ALTER TABLE [table_name] ADD INDEX idx_[column] ([column]);
```

**数据迁移**：
- 迁移脚本路径：`migrations/V{version}__{description}.sql`
- 历史数据处理方案：[说明]
- 是否需要停机：[是/否]，预计时间：[X]min

**事务和一致性**：
- 事务边界：[哪些操作在同一事务]
- 一致性策略：[强一致性/最终一致性/SAGA]
- 并发控制：[乐观锁版本号/悲观锁SELECT FOR UPDATE]

**SQL 执行计划** ⭐🆕：
- 完整 DDL 脚本：[spec-dev/{req}/sql-ddl.md](../../spec-dev/{req}/sql-ddl.md) 第1节
- 完整 DML 脚本：[spec-dev/{req}/sql-ddl.md](../../spec-dev/{req}/sql-ddl.md) 第2节
- 执行顺序：[见 sql-ddl.md 第3节](../../spec-dev/{req}/sql-ddl.md)
- 回滚脚本：[见 sql-ddl.md 第4节](../../spec-dev/{req}/sql-ddl.md)
- SQL 方言：[mysql | postgresql]

### 2.9 中间件设计 ⭐（后端/全栈场景）

**Redis 缓存**：
| 用途 | Key格式 | Value | TTL | 更新策略 |
|------|---------|-------|-----|---------|
| [缓存xxx] | `app:module:{id}` | JSON | 3600s | 写时失效/定时刷新 |

**Nacos 配置**：
| 配置项 | Key | 类型 | 动态刷新 |
|--------|-----|------|---------|
| [配置名] | `module.xxx.enabled` | Boolean | 是/否 |

**Docker/K8s 变更**：
- 资源限制：CPU [X]m, Memory [X]Mi
- 副本数：[X]
- 环境变量变更：[新增/修改]
- ConfigMap/Secret 变更：[说明]

### 2.8 日志和可观测性设计 ⭐（后端/全栈场景）

---

### 2.10 接口契约 ⭐🆕（前后端都改动时必执行）

> **此章节是前后端协作的核心纽带。必须在数据库设计确认后再定义，前后端双方确认后各自独立开发。**

> **🆕 核心原则：参考原有项目架构**。接口设计必须参考原有项目的 API 模式，最大化按照原有方式去设计和实现。仅在原有模式无法满足需求时才引入新模式。

#### 2.10.0 原有 API 模式分析 ⭐🆕

> 在定义新接口之前，先分析现有项目的 API 模式：

| 分析维度 | 现有模式 | 新接口是否遵循 | 说明 |
|---------|---------|-------------|------|
| 路由前缀 | 如 `/api/v1/` | ✅/❌ | [如不遵循，说明原因] |
| 响应包装格式 | 如 `{ code, data, message }` | ✅/❌ | - |
| 认证方式 | 如 Bearer Token / JWT | ✅/❌ | - |
| 鉴权方式 | 如 RBAC / 角色标识 | ✅/❌ | - |
| 错误码格式 | 如 4位数字码 / 字符串码 | ✅/❌ | - |
| 分页参数 | 如 `page/pageSize` | ✅/❌ | - |
| 排序参数 | 如 `sort=field:asc` | ✅/❌ | - |
| 命名风格 | 如 camelCase / snake_case | ✅/❌ | - |
| 时间格式 | 如 ISO 8601 / 时间戳 | ✅/❌ | - |

#### 2.10.1 API 端点总览

| 端点名称 | Method | Path | 描述 | 认证 | 权限 |
|---------|--------|------|------|------|------|
| [端点名] | GET/POST/PUT/DELETE | /api/v1/... | [功能描述] | [认证方式] | [权限要求] |

#### 2.10.2 请求参数规范

**`[Method] [Path]` - [端点描述]**

```typescript
interface [EndpointName]Request {
  // 必填字段
  field1: Type;        // 说明，约束条件
  // 可选字段
  field2?: Type;       // 说明，默认值
}
```

| 字段 | 类型 | 必填 | 说明 | 校验规则 | 示例 |
|------|------|------|------|---------|------|
| field1 | Type | ✅ | [说明] | [约束] | [示例值] |

#### 2.10.3 响应体规范

**成功响应 (200)**：

```typescript
interface [EndpointName]Response {
  code: number;          // 200 = 成功
  data: {
    // 字段必须与数据库设计保持一致
  };
  message: string;
}
```

**错误响应**：
| HTTP 状态码 | 错误码 | 含义 | 前端处理策略 |
|------------|--------|------|-------------|
| 400 | 40001 | 参数校验失败 | 显示表单错误 |
| 401 | 40101 | 未认证 | 跳转登录 |
| 403 | 40301 | 权限不足 | 显示无权限提示 |
| 404 | 40401 | 资源不存在 | 跳转404页 |
| 500 | 50001 | 服务器内部错误 | 显示错误提示 |

#### 2.10.4 Mock 数据示例

> 前端开发使用以下 Mock 数据进行开发，不依赖后端实际实现：

```json
{
  "code": 200,
  "data": {
    "items": [/* 示例数据 */],
    "total": 100
  },
  "message": "success"
}
```

#### 2.10.5 接口确认清单

| 确认方 | 确认项 | 状态 |
|--------|--------|------|
| 后端 | 可以按此契约实现 API | ⏳ |
| 前端 | 可以按此契约实现 UI | ⏳ |
| 双方 | 所有字段与数据库设计保持一致 | ⏳ |
| 双方 | Mock 数据充分满足前端开发需求 | ⏳ |

**关键业务日志**：
| 日志点 | 级别 | 内容 | 上下文 |
|--------|------|------|--------|
| [操作入口] | INFO | [描述] | userId, requestId |
| [操作结果] | INFO/ERROR | [描述] | userId, requestId, result/error |

**链路追踪**：
- traceId 传递方式：HTTP Header `X-Trace-Id`
- 关键链路节点：[网关 → 服务A → 服务B → DB]

### 2.9 样式方案
- **样式框架**：[CSS Modules / Tailwind / Styled Components]
- **主题定制**：[是否支持？如何定制？]
- **响应式**：[考虑的断点和适配方案]

### 2.10 关键交互
- **交互1**：[描述用户交互和系统响应]
- **交互2**：...

### 2.11 前端构建设计 ⭐（前端/全栈场景）

**构建配置**：
| 配置项 | 当前值 | 变更内容 | 原因 |
|--------|--------|---------|------|
| 输出目录 | dist/ | - | - |
| 代码分割 | 路由级 | 新增xxx组件级分割 | 按需加载优化 |

**资源部署策略**：
| 资源类型 | 部署位置 | CDN域名 | Cache-Control |
|---------|---------|---------|--------------|
| HTML入口 | Nginx/服务器 | www.example.com | no-cache |
| JS Bundle | CDN/对象存储 | cdn.example.com | max-age=31536000, immutable |

**性能优化**：
- 代码分割策略：[路由级/组件级/按需]
- 懒加载方案：[IntersectionObserver/路由懒加载]
- 预加载策略：[prefetch/preload/modulepreload]

**浏览器兼容**：
- 目标浏览器：[Browerlist配置]
- Polyfill策略：[按需引入/全量引入/core-js]

### 2.12 前端可观测性设计 ⭐（前端/全栈场景）

**错误追踪**：
| 错误类型 | 捕获方式 | 上报内容 | 告警阈值 |
|---------|---------|---------|---------|
| JS运行时错误 | window.onerror | 文件、行号、堆栈 | 新增错误 > 10次/h |
| Promise异常 | unhandledrejection | Reject值 | 同上 |

**性能监控**：
| 指标 | 目标值 | 采集方式 |
|------|--------|---------|
| LCP | < 2.5s | PerformanceObserver |
| INP | < 200ms | PerformanceObserver |

**用户行为埋点**：
| 埋点 | 触发时机 | 上报内容 |
|------|---------|---------|
| PV | 页面加载 | URL、标题、Referrer |

### 2.13 测试性设计 ⭐（前端/全栈场景）

**data-testid 策略**：
| 组件/页面 | 关键元素 | data-testid | 对应验收标准 |
|-----------|---------|------------|-------------|
| 登录页 | 表单 | login-form | BROWSER-TESTABLE#1 |
| 登录页 | 用户名输入 | login-username | BROWSER-TESTABLE#1 |
| 登录页 | 密码输入 | login-password | BROWSER-TESTABLE#1 |
| 登录页 | 登录按钮 | login-submit | BROWSER-TESTABLE#1 |
| Dashboard | 欢迎信息 | welcome-message | BROWSER-TESTABLE#2 |

**可测试交互路径**：
| 测试场景 | 操作步骤 | 预期结果 | 对应 spec |
|---------|---------|---------|----------|
| 登录流程 | 访问 /login → 填写表单 → 点击提交 → 验证跳转 | URL 变为 /dashboard，显示欢迎信息 | scenarios/login.md |
| 错误处理 | 提交空表单 → 验证错误提示显示 | 显示 "不能为空" 错误提示 | scenarios/login.md#Case2 |
| ... | ... | ... | ... |

**视觉回归关键点**：
| 页面/组件 | 截图类型 | 视口 | 说明 |
|----------|---------|------|------|
| 首页 | 全页面 | 1280x720 | 整体布局验证 |
| 登录表单 | 组件级 | - | 表单渲染验证 |
| ... | ... | ... | ... |

### 2.15 组件/模块抽离策略 ⭐🆕（前端/全栈场景）

> 组件拆分决策和组合策略。

**组件拆分决策**：

| 组件名 | 拆分理由 | 复用性 | 预估复杂度 |
|--------|---------|--------|-----------|
| [组件A] | [单一职责/多处复用/复杂度超标] | [高/中/低] | [简单/中等/复杂] |

**组合策略**：
- 使用组合模式替代继承（has-a vs is-a）
- 通过 Props 和 Slots/Children 组合功能
- 提取共享逻辑为 Composition API / Hooks

**Props/Events 设计规范**：
- Props 数量控制在 5 个以内（超过考虑拆分）
- 所有 Props 必须有明确的类型定义
- Events 命名使用 `onXxx` 或 `emit('xxx')` 风格

---

### 2.16 领域模型设计 ⭐🆕（后端/全栈场景）

> DDD 领域模型定义。

**聚合根定义**：

| 聚合根 | 聚合内对象 | 一致性边界 | 业务规则 |
|--------|-----------|-----------|---------|
| [Order] | [OrderItem, Payment] | [单个订单及其子项] | [订单金额计算、状态转换] |

**实体定义**：

| 实体 | 唯一标识 | 可变属性 | 业务行为 |
|------|---------|---------|---------|
| [Order] | orderId | status, items | cancel(), confirmPayment() |

**值对象定义**：

| 值对象 | 属性 | 不变性 | 相等性比较 |
|--------|------|--------|-----------|
| [Address] | street, city, province, zip | ✅ | 所有属性相等则相等 |
| [Money] | amount, currency | ✅ | amount 和 currency 相等 |

**领域服务定义**：

| 领域服务 | 涉及的领域对象 | 职责 |
|---------|-------------|------|
| [OrderPaymentService] | [Order, Payment] | [处理支付、确认订单] |

---

### 2.14 多项目协作 ⭐🆕（多项目场景必填）

> 当协作模式非 single 时必填。

**协作模式**：[monorepo | multi-repo | same-repo]
**涉及项目**：[project-a, project-b, ...]
**协作计划**：[collaboration-plan.md](../collaboration-plan.md)

**本项目角色**：[提供方 | 消费方 | 两者]
**跨项目接口**：
| 端点 | 方向 | 对方项目 | 状态 |
|------|------|---------|------|
| [API] | 提供/消费 | [project-x] | 待确认 |

**依赖门控**：
| 门控 | 前置项目 | 前置任务 | 验证方式 |
|------|---------|---------|---------|
| [接口X就绪] | [project-a] | [T-A3] | [契约测试通过] |

---

## 3. 与参考设计的对比

### 参考组件
- **名称**：[参考组件名]
- **路径**：[参考组件路径]

### 相同点（完全继承参考设计）
- [相同点1]
- [相同点2]

### 差异点（新增或优化）
- **差异1**：[描述差异 + 原因]
- **差异2**：...

---

## 4. 技术方案

### 4.1 依赖库
| 库 | 用途 | 版本 |
|---|------|------|
| react | UI框架 | ^18.0 |

### 4.2 API设计

#### 获取数据
- **接口**：[API路径]
- **参数**：[参数说明]
- **返回值**：[返回数据结构]

#### 提交数据
- **接口**：[API路径]
- **参数**：[参数说明]
- **返回值**：[返回数据结构]

### 4.3 性能考虑
- **虚拟滚动**：[是否需要？用于何处？]
- **缓存策略**：[数据缓存如何处理？]
- **懒加载**：[是否有懒加载需求？]

### 4.4 监控设计 ⭐（后端/全栈场景）

**API 监控指标**：
| API | 监控指标 | 告警阈值 | 告警方式 |
|-----|---------|---------|---------|
| POST /api/xxx | QPS, P99延迟, 错误率 | 错误率 > 5% | 钉钉/短信 |

### 4.5 上线方案 ⭐（后端/全栈场景）

**上线执行顺序**：
1. 执行数据库迁移脚本
2. 更新配置（Nacos/环境变量）
3. 部署依赖服务（如有）
4. 部署本服务（灰度）
5. 灰度验证
6. 全量部署
7. 上线后验证

**灰度策略**：[灰度方式和比例]
**回滚方案**：[回滚步骤和兼容性处理]

### 4.6 前端上线方案 ⭐（前端/全栈场景）

**上线执行顺序**：
1. 执行生产构建（CI/CD）
2. 构建产物上传至CDN
3. 部署HTML入口文件
4. 清除CDN缓存（如需要）
5. 线上功能验证
6. 监控数据确认

**CDN缓存策略**：
| 资源类型 | 缓存策略 | 更新方式 |
|---------|---------|---------|
| HTML入口 | no-cache | 直接替换 |
| JS/CSS Bundle | max-age=31536000, immutable | 文件名hash变化自动更新 |

### 4.7 前端监控设计 ⭐（前端/全栈场景）

**错误监控**：
| 指标 | 告警阈值 | 告警方式 |
|------|---------|---------|
| JS错误率 | > 5% (5min窗口) | 钉钉+短信 |

**性能告警**：
| 指标 | 告警阈值 | 告警方式 |
|------|---------|---------|
| P95页面加载 | > 5s | 钉钉 |

---

## 5. 实现清单

### 核心组件
- [ ] [组件1]
- [ ] [组件2]

### 工具函数/Hooks
- [ ] [Hook1]
- [ ] [Hook2]

### 样式文件
- [ ] 组件样式
- [ ] 全局样式调整

### 测试
- [ ] 单元测试
- [ ] 集成测试
- [ ] E2E测试

---

## 6. 设计决策记录

### 决策1：[决策主题]
- **问题**：[为什么要做这个决策？]
- **方案对比**：[考虑过什么方案？]
- **最终决策**：[选择了哪个方案？]
- **原因**：[为什么选择这个？]

---

## 7. 潜在风险和注意事项

- **风险1**：[描述风险] - 缓解措施：[如何缓解？]
- **注意事项1**：[需要特别注意什么？]

---

## 8. 下一步

1. 用户审查这个设计方案
2. 确认设计无误后，进入Task阶段
3. /code-task 会将设计转换为具体的代码任务列表
```
